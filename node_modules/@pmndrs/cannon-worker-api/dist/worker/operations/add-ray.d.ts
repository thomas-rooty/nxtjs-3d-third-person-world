import type { CannonMessageMap } from '../../types';
import type { State } from '../state';
export declare const addRay: (state: State, { props: { from, mode, to, ...rayOptions }, uuid }: CannonMessageMap['addRay']) => void;
